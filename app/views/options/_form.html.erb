<%= simple_form_for(@poll, @option) do |o| %>
    <%= o.error_notification %>

    <%= o.input :answer, label: 'What is the answer' %>
    <%= o.input :count, label: 'Count'%>
    <%= o.button :submit %>

<% end %>